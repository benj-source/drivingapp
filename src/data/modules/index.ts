import { priorityModule } from './priority';
import { speedModule } from './speed';
import { signsModule } from './signs';
import { specialModule } from './special';

export { priorityModule, speedModule, signsModule, specialModule };

export const allModules = [priorityModule, speedModule, signsModule, specialModule];
